{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Electron 主进程",
      "program": "${workspaceFolder}/dist/boot/index.js",
      "request": "launch",
      "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/nodemon",
      "skipFiles": [
        "<node_internals>/**"
      ],
      "type": "pwa-node",
      "preLaunchTask": "tsc: watch - tsconfig.json"
    }
  ]
}
